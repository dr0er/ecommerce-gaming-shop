name: Frontend-lint

on: [push]

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd Frontend && npm ci
      - run: cd Frontend && npm run lint
